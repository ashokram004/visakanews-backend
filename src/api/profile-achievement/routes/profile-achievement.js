'use strict';

/**
 * profile-achievement router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::profile-achievement.profile-achievement');
